# WebUI Roadmap

记录 WebUI 未完善的功能（卡片页签内的 Roadmap 区块同步展示部分条目）。

## 卡片（当前页签功能不完整）

- [ ] 编辑 / 删除卡片（引擎无 card_delete 事件，需先设计）
- [ ] 卡片导出 / 导入 UI（引擎已有 `export_card`，UI 未接）
- [ ] 跨会话卡库与引用
- [ ] 树图选节点点选 → 自定义提炼范围生成卡片

## 对话

- [ ] 模型切换 UI（引擎 `/model` 已支持，UI 未接）
- [ ] 流式输出（等 SpecModule `llm.chat()` 补 `on_token`，引擎 spec §10）
- [ ] 会话内消息搜索
- [ ] 置顶 / 拖拽排序
- [ ] 会话按时间分组（当前仅分类分组 + 归档折叠）

## 对话树

- [ ] 节点拖拽改挂载点（引擎 parent 只写不可改，需先设计语义）
- [ ] 子树折叠 / 迷你地图
- [ ] 树图缩放

## 设置（当前仅占位）

- [ ] 主题切换（当前跟随系统，localStorage `treechat.theme` 可手改）
- [ ] data_dir / 窗口预算 / 默认模型配置
- [ ] token 用量统计展示
